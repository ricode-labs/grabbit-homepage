---
const messages = {
	en: {
		htmlLang: "en",
		title: "grabbit - Download made easy",
		description:
			"grabbit is a clean, cute desktop download tool with multi-protocol downloads, task management, resume support, and live speed stats.",
		nav: {
			primary: "Main navigation",
			home: "grabbit home",
			github: "GitHub",
			language: "Language",
		},
		hero: {
			title: 'Download<br />with <em>grabbit</em>',
			lede:
				"A clean desktop downloader for HTTP, FTP, BitTorrent, and Magnet. Manage tasks, resume downloads, and track speed in one cute app.",
			downloadsLabel: "Download options",
			support: "Windows 10+ / macOS 11+ / Linux",
			previewLabel: "grabbit app preview",
		},
		screenshot: {
			title: "grabbit",
		},
		downloadLabels: {
			windows: "Windows",
			linux: "Linux",
			macos: "macOS",
		},
		featuresTitle: 'Why choose <em>grabbit</em>?',
		features: [
			{
				title: "Multi-protocol support",
				body: "HTTP/HTTPS, FTP, BitTorrent, Magnet, and more.",
			},
			{
				title: "Fast downloads",
				body: "Multi-threading and smart scheduling help use your bandwidth efficiently.",
			},
			{
				title: "Resume support",
				body: "Pause or recover from interruptions without losing progress.",
			},
			{
				title: "Task management",
				body: "Categories, sorting, and batch actions keep downloads organized.",
			},
			{
				title: "Clean and safe",
				body: "No ads or bundles. Open, transparent, and privacy-conscious.",
			},
		],
		closing: {
			title: "Make every download<br />easy and pleasant.",
			body: 'grabbit helps you<br /><span>grab</span> what you need with ease.',
		},
		changelog: "Changelog",
	},
	zh: {
		htmlLang: "zh-CN",
		title: "grabbit - 轻松下载，一切尽在 grabbit",
		description:
			"grabbit 是一款简洁可爱的桌面下载工具，支持多协议下载、任务管理、断点续传和实时速度统计。",
		nav: {
			primary: "主导航",
			home: "grabbit 首页",
			github: "GitHub",
			language: "语言",
		},
		hero: {
			title: '轻松下载,<br />一切尽在 <em>grabbit</em>',
			lede: "简洁可爱的桌面下载工具，支持 HTTP、FTP、BT、磁力链接、任务管理和断点续传。",
			downloadsLabel: "下载入口",
			support: "支持 Windows 10+ / macOS 11+ / Linux",
			previewLabel: "grabbit 应用界面预览",
		},
		screenshot: {
			title: "grabbit",
		},
		downloadLabels: {
			windows: "Windows 版",
			linux: "Linux 版",
			macos: "macOS 版",
		},
		featuresTitle: '为什么选择 <em>grabbit</em>？',
		features: [
			{
				title: "多协议支持",
				body: "支持 HTTP/HTTPS、FTP、BT、Magnet 等协议。",
			},
			{
				title: "高速下载",
				body: "多线程加速与智能调度，更高效利用带宽。",
			},
			{
				title: "断点续传",
				body: "暂停或中断后可继续下载，进度不浪费。",
			},
			{
				title: "任务管理",
				body: "分类、排序、批量操作，让任务保持清晰。",
			},
			{
				title: "纯净安全",
				body: "无广告、无捆绑，开源透明，重视隐私与数据安全。",
			},
		],
		closing: {
			title: "让每一次下载，<br />都更轻松愉快。",
			body: '用 grabbit，<br />轻松 <span>grab</span> 住想要的内容。',
		},
		changelog: "更新日志",
	},
	ja: {
		htmlLang: "ja-JP",
		title: "grabbit - かんたんダウンロード",
		description:
			"grabbit は、マルチプロトコルダウンロード、タスク管理、再開対応、リアルタイム速度表示を備えた、シンプルでかわいいデスクトップダウンローダーです。",
		nav: {
			primary: "メインナビゲーション",
			home: "grabbit ホーム",
			github: "GitHub",
			language: "言語",
		},
		hero: {
			title: 'ダウンロードを<br /><em>grabbit</em> でかんたんに',
			lede: "HTTP、FTP、BitTorrent、Magnet に対応した、シンプルでかわいいデスクトップダウンローダーです。タスク管理と再開にも対応。",
			downloadsLabel: "ダウンロード項目",
			support: "Windows 10+ / macOS 11+ / Linux",
			previewLabel: "grabbit アプリのプレビュー",
		},
		screenshot: {
			title: "grabbit",
		},
		downloadLabels: {
			windows: "Windows版",
			linux: "Linux版",
			macos: "macOS版",
		},
		featuresTitle: 'なぜ <em>grabbit</em> なのか？',
		features: [
			{
				title: "マルチプロトコル対応",
				body: "HTTP/HTTPS、FTP、BitTorrent、Magnet などに対応。",
			},
			{
				title: "高速ダウンロード",
				body: "マルチスレッド加速と賢い配分で、回線を効率よく使えます。",
			},
			{
				title: "再開対応",
				body: "一時停止や中断後も、進捗を失わず再開できます。",
			},
			{
				title: "タスク管理",
				body: "分類、並び替え、一括操作で、タスクを整理できます。",
			},
			{
				title: "クリーンで安全",
				body: "広告なし、同梱なし。オープンで、プライバシーにも配慮。",
			},
		],
		closing: {
			title: "ダウンロードを、<br />もっと気持ちよく。",
			body: 'grabbit で、<br />欲しいものを気軽に <span>grab</span>。',
		},
		changelog: "更新履歴",
	},
};

const defaultMessages = messages.en;
const localeNames = {
	en: "English",
	zh: "中文",
	ja: "日本語",
};

const downloads = [
	{
		key: "windows",
		icon: "windows",
		href: "https://github.com/ricode-labs/grabbit/releases/latest/download/Grabbit-windows-x64-setup.exe",
	},
	{
		key: "linux",
		icon: "linux",
		href: "https://github.com/ricode-labs/grabbit/releases/latest/download/Grabbit-linux-x64.flatpak",
	},
	{
		key: "macos",
		icon: "apple",
		href: "https://github.com/ricode-labs/grabbit/releases/latest/download/Grabbit-macos-arm64.pkg",
	},
];

const base = import.meta.env.BASE_URL;
const asset = (path = "") => `${base}${path.replace(/^\/+/, "")}`;
const iconStyles = `--icon-windows: url("${asset("windows.svg")}"); --icon-linux: url("${asset("linux.svg")}"); --icon-apple: url("${asset("apple.svg")}"); --icon-github: url("${asset("github.svg")}"); --icon-protocol: url("${asset("protocol.svg")}"); --icon-cloud-download: url("${asset("cloud-download.svg")}");`;
const screenshotAssets = {
	en: asset("capture-english.png"),
	zh: asset("capture-中文.png"),
	ja: asset("capture-日本語.png"),
};

const features = [
	{
		titleKey: "features.0.title",
		bodyKey: "features.0.body",
		image: "/pending.webp",
		icon: "cloud",
		iconAsset: "protocol",
		tone: "pink",
	},
	{
		titleKey: "features.1.title",
		bodyKey: "features.1.body",
		image: "/downloading.webp",
		icon: "bolt",
		tone: "stone",
	},
	{
		titleKey: "features.2.title",
		bodyKey: "features.2.body",
		image: "/paused.webp",
		icon: "upload",
		iconAsset: "cloud-download",
		tone: "orange",
	},
	{
		titleKey: "features.3.title",
		bodyKey: "features.3.body",
		image: "/pending.webp",
		icon: "list",
		tone: "green",
	},
	{
		titleKey: "features.4.title",
		bodyKey: "features.4.body",
		image: "/downloaded.webp",
		icon: "shield",
		tone: "brown",
		wide: true,
	},
];
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/webp" href={asset("favicon.webp?v=2")} />
		<meta name="viewport" content="width=device-width" />
		<meta name="description" content={defaultMessages.description} />
		<meta name="generator" content={Astro.generator} />
		<title>{defaultMessages.title}</title>
	</head>
	<body style={iconStyles}>
		<main class="page-shell">
			<nav
				class="topbar"
				aria-label={defaultMessages.nav.primary}
				data-i18n="nav.primary"
				data-i18n-mode="attr"
				data-i18n-attr="aria-label"
			>
				<a
					class="brand"
					href={asset("")}
					aria-label={defaultMessages.nav.home}
					data-i18n="nav.home"
					data-i18n-mode="attr"
					data-i18n-attr="aria-label"
				>
					<img src={asset("logo.svg")} alt="" width="36" height="36" />
					<span>grabbit</span>
				</a>

				<div class="nav-links">
					<details class="lang-switcher" data-language-switcher>
						<summary
							class="lang-switcher-button"
							aria-label={defaultMessages.nav.language}
							data-i18n="nav.language"
							data-i18n-mode="attr"
							data-i18n-attr="aria-label"
						>
							<span class="lang-switcher-icon" aria-hidden="true">
								<svg viewBox="0 0 24 24">
									<circle cx="12" cy="12" r="9"></circle>
									<path d="M3 12h18M12 3c3.2 3.5 3.2 14.5 0 18M12 3c-3.2 3.5-3.2 14.5 0 18"></path>
								</svg>
							</span>
						</summary>
						<div class="lang-switcher-menu" aria-label={defaultMessages.nav.language}>
							{Object.entries(localeNames).map(([locale, label]) => (
								<button
									class="lang-switcher-option"
									type="button"
									data-locale-option
									data-locale={locale}
									aria-pressed={locale === "en" ? "true" : "false"}
								>
									<span>{label}</span>
								</button>
							))}
						</div>
					</details>
					<a class="github-link" href="https://github.com/ricode-labs/grabbit" target="_blank" rel="noreferrer">
						<span class="github-icon" aria-hidden="true"></span>
						<span data-i18n="nav.github">{defaultMessages.nav.github}</span>
					</a>
				</div>
			</nav>

			<section class="hero" aria-labelledby="hero-title">
				<div class="hero-copy">
					<h1 id="hero-title" data-i18n="hero.title" data-i18n-mode="html" set:html={defaultMessages.hero.title}></h1>
					<p class="hero-lede" data-i18n="hero.lede">{defaultMessages.hero.lede}</p>

					<div
						class="download-actions"
						id="downloads"
						aria-label={defaultMessages.hero.downloadsLabel}
						data-i18n="hero.downloadsLabel"
						data-i18n-mode="attr"
						data-i18n-attr="aria-label"
					>
						{downloads.map((download) => (
							<a
								class="download-button"
								href={download.href}
							>
								<span class={`platform-icon platform-icon-${download.icon}`} aria-hidden="true"></span>
								<strong data-i18n={`downloadLabels.${download.key}`}>
									{defaultMessages.downloadLabels[download.key]}
								</strong>
							</a>
						))}
					</div>

					<p class="support-note" data-i18n="hero.support">
						{defaultMessages.hero.support}
					</p>
				</div>

				<div
					class="app-preview"
					aria-label={defaultMessages.hero.previewLabel}
					data-i18n="hero.previewLabel"
					data-i18n-mode="attr"
					data-i18n-attr="aria-label"
				>
					<img
						src={screenshotAssets.en}
						alt={defaultMessages.hero.previewLabel}
						width="1600"
						height="1200"
						loading="eager"
						data-i18n="hero.previewLabel"
						data-i18n-mode="attr"
						data-i18n-attr="alt"
						data-locale-screenshot
					/>
				</div>
			</section>

			<section class="features" id="features" aria-labelledby="features-title">
				<h2 id="features-title">
					<span>✿</span>
					<span data-i18n="featuresTitle" data-i18n-mode="html" set:html={defaultMessages.featuresTitle}></span>
					<span>✿</span>
				</h2>

				<div class="feature-grid">
					{features.map((feature, index) => (
						<article class:list={["feature-card", feature.wide && "feature-card-wide"]}>
							<div class={`feature-icon feature-icon-${feature.tone}`} aria-hidden="true">
								{feature.iconAsset && <span class={`asset-icon asset-icon-${feature.iconAsset}`}></span>}
								{!feature.iconAsset && feature.icon === "cloud" && (
									<svg viewBox="0 0 24 24"><path d="M12 5v10m0 0-4-4m4 4 4-4" /><path d="M6 18h11a4 4 0 0 0 .8-7.92A6 6 0 0 0 6.2 8.7 4.7 4.7 0 0 0 6 18Z" /></svg>
								)}
								{!feature.iconAsset && feature.icon === "bolt" && (
									<svg viewBox="0 0 24 24"><path d="M13 2 4 14h7l-1 8 10-13h-7l0-7Z" /></svg>
								)}
								{!feature.iconAsset && feature.icon === "upload" && (
									<svg viewBox="0 0 24 24"><path d="M12 19V9m0 0-4 4m4-4 4 4" /><path d="M6 18H5a4 4 0 0 1 0-8 7 7 0 0 1 13.5-2A5 5 0 0 1 18 18h-1" /></svg>
								)}
								{!feature.iconAsset && feature.icon === "list" && (
									<svg viewBox="0 0 24 24"><path d="M8 6h11M8 12h11M8 18h11" /><path d="M4 6h.01M4 12h.01M4 18h.01" /></svg>
								)}
								{!feature.iconAsset && feature.icon === "shield" && (
									<svg viewBox="0 0 24 24"><path d="M12 3 5 6v6c0 4.5 2.8 7.6 7 9 4.2-1.4 7-4.5 7-9V6l-7-3Z" /><path d="m9 12 2 2 4-5" /></svg>
								)}
							</div>
							<div class="feature-copy">
								<h3 data-i18n={feature.titleKey}>{defaultMessages.features[index].title}</h3>
								<p data-i18n={feature.bodyKey}>{defaultMessages.features[index].body}</p>
							</div>
							<img src={asset(feature.image)} alt="" width="170" height="170" />
							<span class="flower flower-left" aria-hidden="true"></span>
							<span class="flower flower-right" aria-hidden="true"></span>
						</article>
					))}
				</div>
			</section>

			<section class="closing" id="help" aria-labelledby="closing-title">
				<img src={asset("paused.webp")} alt="" width="210" height="210" />
				<div>
					<h2 id="closing-title" data-i18n="closing.title" data-i18n-mode="html" set:html={defaultMessages.closing.title}></h2>
					<p data-i18n="closing.body" data-i18n-mode="html" set:html={defaultMessages.closing.body}></p>
				</div>
				<img src={asset("downloaded.webp")} alt="" width="210" height="210" />
			</section>

			<div id="changelog" class="sr-only" data-i18n="changelog">{defaultMessages.changelog}</div>
		</main>

		<script is:inline define:vars={{ messages, localeNames, screenshotAssets }}>
			const storageKey = "grabbit-locale";
			const validLocales = new Set(Object.keys(messages));
			const localeSwitcher = document.querySelector("[data-language-switcher]");
			const localeButtons = document.querySelectorAll("[data-locale-option]");
			const screenshot = document.querySelector("[data-locale-screenshot]");

			const normalizeLocale = (value) => {
				if (!value) {
					return undefined;
				}

				const normalized = String(value).toLowerCase();
				if (normalized.startsWith("zh")) {
					return "zh";
				}

				if (normalized.startsWith("ja")) {
					return "ja";
				}

				if (normalized.startsWith("en")) {
					return "en";
				}

				return undefined;
			};

			const getStoredLocale = () => {
				try {
					return normalizeLocale(localStorage.getItem(storageKey));
				} catch {
					return undefined;
				}
			};

			const setStoredLocale = (locale) => {
				try {
					localStorage.setItem(storageKey, locale);
				} catch {
					// Ignore storage failures.
				}
			};

			const pickLocale = () => {
				const storedLocale = getStoredLocale();
				if (storedLocale) {
					return storedLocale;
				}

				const candidates = navigator.languages?.length ? navigator.languages : [navigator.language];
				for (const candidate of candidates) {
					const locale = normalizeLocale(candidate);
					if (locale) {
						return locale;
					}
				}

				return "en";
			};

			const getPath = (source, path) =>
				path.split(".").reduce((value, key) => {
					if (!value || typeof value !== "object") {
						return undefined;
					}

					return key in value ? value[key] : undefined;
				}, source);

			const applyLocale = (locale, { persist = false } = {}) => {
				const nextLocale = validLocales.has(locale) ? locale : "en";
				const strings = messages[nextLocale] ?? messages.en;

				document.documentElement.lang = strings.htmlLang;
				document.documentElement.dataset.locale = nextLocale;
				document.title = strings.title;

				if (screenshot) {
					screenshot.setAttribute("src", screenshotAssets[nextLocale] ?? screenshotAssets.en);
				}

				const description = document.querySelector('meta[name="description"]');
				if (description) {
					description.setAttribute("content", strings.description);
				}

				document.querySelectorAll("[data-i18n]").forEach((node) => {
					const key = node.dataset.i18n;
					if (!key) {
						return;
					}

					const value = getPath(strings, key) ?? getPath(messages.en, key);
					if (typeof value !== "string") {
						return;
					}

					const mode = node.dataset.i18nMode ?? "text";
					if (mode === "html") {
						node.innerHTML = value;
					} else if (mode !== "attr") {
						node.textContent = value;
					}

					const attr = node.dataset.i18nAttr;
					if (attr) {
						node.setAttribute(attr, value);
					}
				});

				localeButtons.forEach((button) => {
					const isActive = button.dataset.locale === nextLocale;
					button.setAttribute("aria-pressed", String(isActive));
				});

				if (persist) {
					setStoredLocale(nextLocale);
				}

				if (localeSwitcher) {
					localeSwitcher.removeAttribute("open");
				}
			};

			applyLocale(pickLocale());

			localeButtons.forEach((button) => {
				button.addEventListener("click", () => {
					const nextLocale = normalizeLocale(button.dataset.locale);
					if (!nextLocale) {
						return;
					}

					applyLocale(nextLocale, { persist: true });
				});
			});
		</script>
	</body>
</html>

<style>
	:root {
		color-scheme: light;
		--brand-cream: #fffbf7;
		--brand-pink-soft: #ffe6ec;
		--brand-pink: #ffc7d4;
		--brand-orange: #f7a94a;
		--brand-green: #79c96b;
		--brand-stone: #bdb0a6;
		--brand-brown: #6b5448;
		--ink: var(--brand-brown);
		--ink-strong: color-mix(in srgb, var(--brand-brown) 86%, black);
		--muted: var(--brand-stone);
		--line: var(--brand-pink-soft);
		--surface: rgba(255, 251, 247, 0.78);
		--surface-solid: var(--brand-cream);
		--cream: var(--brand-cream);
		--pink: var(--brand-pink);
		--pink-deep: color-mix(in srgb, var(--brand-pink) 76%, var(--brand-brown));
		--pink-soft: var(--brand-pink-soft);
		--green: var(--brand-green);
		--orange: var(--brand-orange);
		--violet: var(--brand-stone);
		--shadow: 0 22px 70px color-mix(in srgb, var(--brand-brown) 18%, transparent);
		font-family:
			"Hiragino Sans", "Yu Gothic", "Meiryo", "Microsoft YaHei", "PingFang SC", "Xiaolai SC",
			"LXGW WenKai", "Kaiti SC", "KaiTi", system-ui, sans-serif;
		background: var(--cream);
		color: var(--ink);
	}

	* {
		box-sizing: border-box;
	}

	html {
		scroll-behavior: smooth;
	}

	body {
		margin: 0;
		min-width: 320px;
		background:
			radial-gradient(circle at 18% 8%, color-mix(in srgb, var(--brand-pink) 28%, transparent), transparent 28rem),
			radial-gradient(circle at 78% 16%, color-mix(in srgb, var(--brand-orange) 18%, transparent), transparent 26rem),
			linear-gradient(180deg, var(--brand-cream) 0%, #fff 58%, var(--brand-cream) 100%);
	}

	body::before {
		content: "";
		position: fixed;
		inset: 0;
		pointer-events: none;
		border: 1px solid var(--line);
		border-radius: 22px;
		z-index: 10;
	}

	a {
		color: inherit;
		text-decoration: none;
	}

	button,
	input {
		font: inherit;
	}

	img,
	svg {
		display: block;
	}

	.page-shell {
		width: min(1200px, calc(100% - 56px));
		margin: 0 auto;
		padding: 30px 0;
	}

	.topbar {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 24px;
		min-height: 48px;
	}

	.brand {
		display: inline-flex;
		align-items: center;
		gap: 10px;
		font-size: 1.72rem;
		font-weight: 800;
		letter-spacing: 0;
		color: var(--ink);
	}

	.brand img {
		width: 36px;
		height: 36px;
		object-fit: contain;
	}

	.nav-links {
		display: flex;
		align-items: center;
		gap: 18px;
		justify-content: flex-end;
		font-family: inherit;
		font-size: 0.94rem;
		color: var(--ink);
	}

	.nav-links a {
		white-space: nowrap;
	}

	.github-link {
		display: inline-flex;
		align-items: center;
		gap: 8px;
		color: var(--ink);
		font-weight: 700;
	}

	.lang-switcher {
		position: relative;
		flex: 0 0 auto;
	}

	.lang-switcher > summary {
		list-style: none;
	}

	.lang-switcher > summary::-webkit-details-marker {
		display: none;
	}

	.lang-switcher-button {
		display: grid;
		place-items: center;
		width: 24px;
		height: 24px;
		border: 0;
		padding: 0;
		background: transparent;
		box-shadow: none;
		color: var(--ink);
		cursor: pointer;
		transition:
			transform 160ms ease,
			opacity 160ms ease;
	}

	.lang-switcher-button:hover {
		transform: translateY(-1px);
		opacity: 0.84;
	}

	.lang-switcher-button:focus-visible {
		outline: 2px solid var(--brand-orange);
		outline-offset: 3px;
	}

	.lang-switcher-icon svg {
		width: 24px;
		height: 24px;
		fill: none;
		stroke: currentColor;
		stroke-width: 1.8;
		stroke-linecap: round;
		stroke-linejoin: round;
	}

	.lang-switcher-menu {
		position: absolute;
		top: calc(100% + 10px);
		right: 0;
		z-index: 20;
		display: grid;
		gap: 6px;
		min-width: 148px;
		border: 1px solid var(--line);
		border-radius: 14px;
		padding: 8px;
		background: color-mix(in srgb, var(--brand-cream) 88%, white);
		box-shadow: 0 18px 42px color-mix(in srgb, var(--brand-brown) 14%, transparent);
		opacity: 0;
		pointer-events: none;
		transform: translateY(-6px);
		transition:
			opacity 140ms ease,
			transform 140ms ease;
	}

	.lang-switcher[open] .lang-switcher-menu {
		opacity: 1;
		pointer-events: auto;
		transform: translateY(0);
	}

	.lang-switcher-option {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 12px;
		width: 100%;
		border: 0;
		border-radius: 10px;
		padding: 9px 10px;
		background: transparent;
		color: var(--ink);
		cursor: pointer;
		font-size: 0.9rem;
		font-weight: 800;
		text-align: left;
		white-space: nowrap;
	}

	.lang-switcher-option:hover,
	.lang-switcher-option[aria-pressed="true"] {
		background: var(--brand-pink-soft);
	}

	.lang-switcher-option::after {
		content: "";
		flex: 0 0 auto;
		width: 8px;
		height: 8px;
		border-radius: 999px;
		background: transparent;
	}

	.lang-switcher-option[aria-pressed="true"]::after {
		background: currentColor;
	}

	.github-icon {
		display: block;
		flex: 0 0 auto;
		width: 24px;
		height: 24px;
		background: currentColor;
		mask-image: var(--icon-github);
		mask-position: center;
		mask-repeat: no-repeat;
		mask-size: contain;
		-webkit-mask-image: var(--icon-github);
		-webkit-mask-position: center;
		-webkit-mask-repeat: no-repeat;
		-webkit-mask-size: contain;
	}

	.feature-icon svg {
		width: 20px;
		height: 20px;
		fill: none;
		stroke: currentColor;
		stroke-width: 2;
		stroke-linecap: round;
		stroke-linejoin: round;
	}

	.hero {
		display: grid;
		grid-template-columns: minmax(320px, 0.82fr) minmax(520px, 1.18fr);
		gap: 62px;
		align-items: center;
		padding: 78px 22px 64px;
	}

	.hero-copy h1 {
		position: relative;
		width: fit-content;
		max-width: 100%;
		margin: 0 0 28px;
		font-size: clamp(2.85rem, 5vw, 4.35rem);
		line-height: 1.22;
		letter-spacing: 0;
		color: var(--ink);
		font-weight: 900;
	}

	.hero-copy h1 span {
		display: block;
	}

	em {
		color: var(--pink);
		font-style: normal;
	}

	.hero-copy h1::after {
		content: "✦";
		position: absolute;
		right: -34px;
		bottom: 0.42em;
		color: var(--brand-orange);
		font-size: 1.35rem;
	}

	:root[data-locale="ja"] .hero-copy h1 {
		font-size: clamp(2.55rem, 4.5vw, 3.8rem);
		line-height: 1.26;
	}

	.hero-lede {
		max-width: 510px;
		margin: 0 0 34px;
		color: var(--ink);
		font-family: inherit;
		font-size: 1rem;
		line-height: 1.9;
	}

	.download-actions {
		display: grid;
		grid-template-columns: minmax(0, 1.08fr) minmax(0, 0.92fr);
		gap: 20px 14px;
		max-width: 410px;
	}

	.download-button {
		display: flex;
		align-items: center;
		gap: 12px;
		min-height: 78px;
		border: 1px solid var(--line);
		border-radius: 15px;
		padding: 14px 18px;
		background-color: color-mix(in srgb, var(--brand-cream) 82%, white);
		background-image: none;
		color: var(--ink);
		font-family: inherit;
		text-decoration: none;
		box-shadow: 0 14px 35px color-mix(in srgb, var(--brand-brown) 8%, transparent);
		cursor: pointer;
		transition:
			background 160ms ease,
			border-color 160ms ease,
			transform 160ms ease,
			box-shadow 160ms ease;
	}

	.download-button:hover,
	.download-button:focus-visible {
		border-color: transparent;
		background-color: var(--brand-pink-soft);
		background-image: linear-gradient(180deg, var(--brand-pink), var(--brand-pink-soft));
		transform: translateY(-2px);
		box-shadow: 0 18px 36px color-mix(in srgb, var(--brand-pink) 48%, transparent);
	}

	.download-button:focus-visible {
		outline: 2px solid var(--brand-orange);
		outline-offset: 4px;
	}

	.download-button:nth-child(3) {
		width: 224px;
		grid-column: 1 / -1;
	}

	.download-button strong {
		display: block;
	}

	.download-button strong {
		font-size: 1rem;
		font-weight: 800;
	}

	.platform-icon {
		display: block;
		flex: 0 0 auto;
		width: 34px;
		height: 34px;
		background: currentColor;
		mask-position: center;
		mask-repeat: no-repeat;
		mask-size: contain;
		-webkit-mask-position: center;
		-webkit-mask-repeat: no-repeat;
		-webkit-mask-size: contain;
	}

	.platform-icon-windows {
		mask-image: var(--icon-windows);
		-webkit-mask-image: var(--icon-windows);
	}

	.platform-icon-linux {
		mask-image: var(--icon-linux);
		-webkit-mask-image: var(--icon-linux);
	}

	.platform-icon-apple {
		mask-image: var(--icon-apple);
		-webkit-mask-image: var(--icon-apple);
	}

	.support-note {
		display: flex;
		align-items: center;
		gap: 8px;
		margin: 34px 0 0;
		color: var(--muted);
		font-family: inherit;
		font-size: 0.86rem;
	}

	.app-preview {
		min-width: 0;
	}

	.app-preview img {
		display: block;
		max-width: 100%;
		height: auto;
	}

	.features {
		padding: 6px 0 30px;
	}

	.features h2 {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 18px;
		margin: 0 0 28px;
		color: var(--ink);
		font-size: clamp(1.82rem, 3.5vw, 2.42rem);
		line-height: 1.2;
		letter-spacing: 0;
		text-align: center;
	}

	.features h2 span {
		color: var(--brand-pink);
		font-size: 1.35rem;
	}

	.feature-grid {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 20px 26px;
	}

	.feature-card {
		position: relative;
		overflow: hidden;
		display: grid;
		grid-template-columns: 76px minmax(0, 1fr) 176px;
		align-items: center;
		min-height: 218px;
		border: 1px solid var(--line);
		border-radius: 22px;
		padding: 26px;
		background: color-mix(in srgb, var(--brand-cream) 68%, white);
		box-shadow: 0 18px 48px color-mix(in srgb, var(--brand-brown) 7%, transparent);
	}

	.feature-card-wide {
		grid-column: 1 / -1;
		grid-template-columns: minmax(76px, 0.22fr) minmax(0, 1fr) 220px;
		min-height: 136px;
	}

	.feature-icon {
		display: grid;
		place-items: center;
		width: 66px;
		height: 66px;
		border-radius: 50%;
		box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--brand-brown) 8%, transparent);
		color: var(--brand-cream);
	}

	.feature-icon-pink {
		background: color-mix(in srgb, var(--brand-pink) 76%, var(--brand-brown));
	}

	.feature-icon-stone {
		background: var(--brand-stone);
	}

	.feature-icon-orange {
		background: var(--brand-orange);
	}

	.feature-icon-green {
		background: var(--brand-green);
	}

	.feature-icon-brown {
		background: var(--brand-brown);
		box-shadow: none;
	}

	.feature-icon svg {
		width: 32px;
		height: 32px;
	}

	.asset-icon {
		display: block;
		width: 32px;
		height: 32px;
		background: currentColor;
		mask-position: center;
		mask-repeat: no-repeat;
		mask-size: contain;
		-webkit-mask-position: center;
		-webkit-mask-repeat: no-repeat;
		-webkit-mask-size: contain;
	}

	.asset-icon-protocol {
		mask-image: var(--icon-protocol);
		-webkit-mask-image: var(--icon-protocol);
	}

	.asset-icon-cloud-download {
		mask-image: var(--icon-cloud-download);
		-webkit-mask-image: var(--icon-cloud-download);
	}

	.feature-copy {
		min-width: 0;
		padding: 0 18px;
	}

	.feature-copy h3 {
		margin: 0 0 14px;
		color: var(--ink);
		font-size: 1.32rem;
		line-height: 1.2;
		letter-spacing: 0;
	}

	:root[data-locale="ja"] .feature-copy h3 {
		font-size: 1.2rem;
	}

	.feature-copy p {
		margin: 0;
		max-width: 300px;
		color: var(--ink);
		font-family: inherit;
		font-size: 0.96rem;
		line-height: 1.8;
	}

	:root[data-locale="ja"] .feature-copy p {
		font-size: 0.9rem;
		line-height: 1.7;
	}

	.feature-card > img {
		justify-self: center;
		width: 160px;
		height: 160px;
		object-fit: contain;
	}

	.feature-card-wide > img {
		width: 150px;
		height: 130px;
	}

	.flower {
		position: absolute;
		bottom: 22px;
		width: 30px;
		height: 30px;
		background:
			radial-gradient(circle at 50% 20%, var(--brand-pink) 0 4px, transparent 4.5px),
			radial-gradient(circle at 80% 50%, var(--brand-pink) 0 4px, transparent 4.5px),
			radial-gradient(circle at 50% 80%, var(--brand-pink) 0 4px, transparent 4.5px),
			radial-gradient(circle at 20% 50%, var(--brand-pink) 0 4px, transparent 4.5px),
			radial-gradient(circle at 50% 50%, var(--brand-orange) 0 4px, transparent 4.5px);
		opacity: 0.8;
	}

	.flower::after {
		content: "";
		position: absolute;
		left: 12px;
		top: 20px;
		width: 3px;
		height: 26px;
		border-radius: 999px;
		background: var(--brand-green);
	}

	.flower-left {
		left: 22px;
		transform: scale(0.55) rotate(-12deg);
	}

	.flower-right {
		right: 38px;
		transform: scale(0.5) rotate(13deg);
	}

	.closing {
		display: grid;
		grid-template-columns: 230px minmax(0, 1fr) 230px;
		align-items: center;
		gap: 18px;
		margin: 26px 0 0;
		border: 1px solid var(--line);
		border-radius: 25px;
		padding: 28px 34px;
		background: color-mix(in srgb, var(--brand-cream) 70%, white);
		text-align: center;
		box-shadow: 0 18px 48px color-mix(in srgb, var(--brand-brown) 7%, transparent);
	}

	.closing img {
		width: 210px;
		height: 210px;
		object-fit: contain;
	}

	.closing h2 {
		margin: 0 0 20px;
		color: var(--ink);
		font-size: clamp(1.72rem, 3.4vw, 2.45rem);
		line-height: 1.65;
		letter-spacing: 0;
		font-weight: 800;
	}

	.closing p {
		margin: 0;
		color: var(--ink);
		font-size: 1.14rem;
		line-height: 1.8;
	}

	.closing span {
		color: var(--pink);
		font-weight: 800;
	}

	.sr-only {
		position: absolute;
		overflow: hidden;
		width: 1px;
		height: 1px;
		margin: -1px;
		padding: 0;
		border: 0;
		clip: rect(0, 0, 0, 0);
		white-space: nowrap;
	}

	@media (max-width: 1080px) {
		.page-shell {
			width: min(100% - 34px, 980px);
		}

		.nav-links {
			gap: 16px;
		}

		.hero {
			grid-template-columns: 1fr;
			gap: 42px;
			padding-inline: 0;
			padding-top: 54px;
		}

		.hero-copy {
			max-width: 650px;
		}

		.feature-card {
			grid-template-columns: 70px minmax(0, 1fr) 140px;
			padding: 22px;
		}

		.feature-card > img {
			width: 132px;
			height: 132px;
		}
	}

	@media (max-width: 820px) {
		body::before {
			border-radius: 16px;
		}

		.topbar {
			align-items: flex-start;
			flex-wrap: wrap;
		}

		.nav-links {
			gap: 12px;
			margin-left: auto;
		}

		.hero {
			padding-top: 38px;
		}

		.download-actions {
			grid-template-columns: 1fr;
			max-width: 430px;
		}

		.download-button:nth-child(3) {
			width: 100%;
			grid-column: auto;
		}

		.feature-grid {
			grid-template-columns: 1fr;
		}

		.feature-card,
		.feature-card-wide {
			grid-column: auto;
			grid-template-columns: 62px minmax(0, 1fr) 112px;
			min-height: 178px;
			padding: 20px 16px;
		}

		.feature-icon {
			width: 56px;
			height: 56px;
		}

		.feature-icon svg {
			width: 27px;
			height: 27px;
		}

		.feature-copy {
			padding: 0 10px;
		}

		.feature-copy h3 {
			font-size: 1.12rem;
		}

		.feature-copy p {
			font-size: 0.86rem;
		}

		.feature-card > img,
		.feature-card-wide > img {
			width: 106px;
			height: 106px;
		}

		.closing {
			grid-template-columns: 1fr;
			padding: 24px 18px;
		}

		.closing img {
			width: 150px;
			height: 150px;
			justify-self: center;
		}
	}

	@media (max-width: 540px) {
		.page-shell {
			width: min(100% - 24px, 480px);
			padding-top: 20px;
		}

		.brand {
			font-size: 1.4rem;
		}

		.nav-links {
			gap: 10px;
		}

		.lang-switcher-button {
			width: 24px;
			height: 24px;
		}

		.hero-copy h1 {
			font-size: clamp(2.45rem, 13vw, 3.4rem);
		}

		.hero-copy h1::after {
			right: -22px;
		}

		:root[data-locale="ja"] .hero-copy h1 {
			font-size: clamp(2.2rem, 10vw, 2.95rem);
		}

		.features h2 {
			gap: 9px;
			font-size: 1.58rem;
		}

		.feature-card,
		.feature-card-wide {
			grid-template-columns: 54px minmax(0, 1fr);
		}

		.feature-card > img,
		.feature-card-wide > img {
			position: absolute;
			right: 6px;
			bottom: 4px;
			width: 84px;
			height: 84px;
			opacity: 0.42;
		}

		.feature-copy p {
			padding-right: 14px;
		}
	}
</style>
